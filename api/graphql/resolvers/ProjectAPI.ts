import { Arg, Ctx, Field, InputType, Mutation, Query, Resolver } from "type-graphql";
import { Context } from "../../types/context";


@Resolver()
export class ProjectAPIResolver {
  // Add custom Project resolvers here
}
